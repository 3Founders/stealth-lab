# Restored: this file was deleted in 10604cf, which is why __pycache__
# entries got committed in that same commit and why .env has been sitting
# untracked-but-not-ignored ever since.

# Secrets. .env holds a live database password and API keys.
.env
.env.local
*.env.local

__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Local caches produced by repo_execution.py's prefetch stage
repo_cache/
