# Built by actually parsing every real import across backend/ with Python's
# ast module (not reconstructed from memory) -- see the extraction method in
# the conversation this was generated from if this ever needs regenerating.
#
# Two real entries here were NOT found by import-parsing and were added from
# direct knowledge instead, since pip install alone can't discover them:
#   - uvicorn: launches app/main.py's real FastAPI app from the command line
#     (`uvicorn app.main:app`), never `import`ed directly in any .py file
#   - mcp[cli]: the [cli] extra specifically, needed for `mcp dev`/`mcp run`
#     (the Inspector, used to test app/mcp_server/server.py) -- bare `mcp`
#     alone does not include this
#
# >=2.0.0 is REQUIRED, not aspirational: app/mcp_server/server.py imports
# MCPServer from mcp.server and mcp.server.mcpserver.Context, neither of
# which exist in the 1.x line (confirmed by a real failing import against
# 1.29.0 -- 1.x ships FastMCP/Server instead). 2.0.0 is the current stable
# release on PyPI as of this pin, not a prerelease.

anthropic
asyncpg
fastapi
huggingface-hub
mcp[cli]>=2.0.0
numpy
openai
openpyxl
pdfplumber
pydantic
pydantic-settings
pytest
python-dotenv
PyYAML
reportlab
scipy
tree-sitter-language-pack
uvicorn
voyageai
