# Built by actually parsing every real import across backend/ with Python's
# ast module (not reconstructed from memory) -- see the extraction method in
# the conversation this was generated from if this ever needs regenerating.
#
# Two real entries here were NOT found by import-parsing and were added from
# direct knowledge instead, since pip install alone can't discover them:
#   - uvicorn: launches app/main.py's real FastAPI app from the command line
#     (`uvicorn app.main:app`), never `import`ed directly in any .py file
#   - mcp[cli]: the [cli] extra specifically, needed for `mcp dev`/`mcp run`
#     (the Inspector, used to test app/mcp_server/server.py) -- bare `mcp`
#     alone does not include this
#
# >=2.0.0 is REQUIRED, not aspirational: app/mcp_server/server.py imports
# MCPServer from mcp.server and mcp.server.mcpserver.Context, neither of
# which exist in the 1.x line (confirmed by a real failing import against
# 1.29.0 -- 1.x ships FastMCP/Server instead). 2.0.0 is the current stable
# release on PyPI as of this pin, not a prerelease.

 anthropic
 asyncpg
 fastapi
 huggingface-hub
 mcp[cli]>=2.0.0
 numpy
 openai
 openpyxl
 pdfplumber
 pydantic
 pydantic-settings
 pytest
 pytest-asyncio
 python-docx
 python-dotenv
 PyJWT[crypto]
 PyYAML
reportlab
scipy
tree-sitter-language-pack
uvicorn
voyageai
z3-solver
google-genai

# undeclared transitive deps relied on by tests/experiments (review 2026-08-24)
pandas
pyarrow

# numeric invariant solver - required by services/invariants.py (was undeclared)
z3-solver

# Observability (app/observability.py) - errors + traces for the two ASGI
# apps and the headless worker. Optional at runtime: no SENTRY_DSN means
# init() is a no-op and this is never imported.
sentry-sdk[fastapi]

# OpenTelemetry tracing (app/telemetry.py). Optional at runtime: with
# OBSERVABILITY_ENABLED unset every span is a no-op.
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http

# python-docx (DocxAdapter, app/services/ingestion_sources/document_adapters/
# docx_adapter.py) - reads .docx paragraphs/tables for the document ingestion
# SourceAdapter layer. pdfplumber above already covers PDF for the same layer.

# Pillow (app/services/avatar.py) - decodes/normalizes uploaded profile
# pictures (real image content, not just the client's claimed MIME type),
# strips EXIF, and re-encodes to WebP before the bytes reach ObjectStore.
Pillow

# Local project sync (app/stealth/local_registry.py, app/stealth/
# local_key_store.py, app/stealth/local_sync_encrypt.py) - see
# docs/local_project_sync_security.md. platformdirs: the standard,
# cross-platform way to find the per-OS user-scoped app-data directory for
# the local project registry (never %APPDATA%/~/.config hardcoded).
# keyring: OS-secure credential storage (Windows Credential Manager /
# macOS Keychain / Linux Secret Service) for the P-DEK and sync device
# credential cache -- never a plaintext file. cryptography: already an
# indirect dependency (PyJWT's RS256/ES256 support); pinned here directly
# too because local_sync_encrypt.py now depends on its AESGCM primitive
# for real, not just transitively -- the SAME AES-256-GCM construction
# the browser uses via Web Crypto, so ciphertext either side produces is
# decryptable by the other.
platformdirs
keyring
cryptography
