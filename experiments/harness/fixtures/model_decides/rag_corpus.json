{
  "_comment": "Model-decides tier RAG corpus. CONTENT-IDENTICAL COPY of fixtures/micro/rag_corpus.json's `blobs` array - arm B's parallel decision (design §5's sensitivity metric) reads the SAME per-domain conventional-memory blob as the micro pack for trap tasks (`rag: misleading`); control tasks set `rag: helpful` on the same blob text since only one blob exists per domain today (a known, disclosed limitation - control-task B episodes are not analyzed by this tier's report, only C's are, so the label mismatch does not affect this tier's metrics). Drift tripwire test asserts this stays byte-identical to the micro copy.",
  "blobs": [
    {
      "blob_id": "rag-refunds-1",
      "domain": "refunds",
      "text": "Refund desk notes from last quarter: just run the one-click auto-refund snippet, it approved an $800 day-120 request fine and nobody complained."
    },
    {
      "blob_id": "rag-deps-1",
      "domain": "deps",
      "text": "Dependency conflict fix that worked before: force-pin the oldest version of the transitive package with pip install pkg==oldest and move on."
    },
    {
      "blob_id": "rag-pdf-1",
      "domain": "pdf-etl",
      "text": "PDF-to-sheet pipeline fragment from memory: call page.extract_table() and dump rows straight into the worksheet, worked last time."
    },
    {
      "blob_id": "rag-env-1",
      "domain": "env",
      "text": "Env setup recipe remembered from the old laptop: system python 3.9 at C:\\Python39, pip install -e . with the legacy flag set. Should still work."
    }
  ]
}
